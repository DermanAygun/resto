<form method="post" action="{{ path('menu_gerecht_delete', {'id': menu_gerecht.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ menu_gerecht.id) }}">
    <button class="btn">Delete</button>
</form>
