{% extends 'base.html.twig' %}

{% block title %}Bon index{% endblock %}

{% block body %}
    <h1>Bon index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Datum</th>
                <th>Time</th>
                <th>Pdf</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for bon in bons %}
            <tr>
                <td>{{ bon.id }}</td>
                <td>{{ bon.datum ? bon.datum|date('Y-m-d') : '' }}</td>
                <td>{{ bon.time ? bon.time|date('H:i:s') : '' }}</td>
                <td>{{ bon.pdf }}</td>
                <td>
                    <a href="{{ path('bon_show', {'id': bon.id}) }}">show</a>
                    <a href="{{ path('bon_edit', {'id': bon.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('bon_new') }}">Create new</a>
{% endblock %}
