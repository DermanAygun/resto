{% extends 'base.html.twig' %}

{% block title %}Bon{% endblock %}

{% block body %}
    <h1>Bon</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ bon.id }}</td>
            </tr>
            <tr>
                <th>Datum</th>
                <td>{{ bon.datum ? bon.datum|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Time</th>
                <td>{{ bon.time ? bon.time|date('H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Pdf</th>
                <td>{{ bon.pdf }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('bon_index') }}">back to list</a>

    <a href="{{ path('bon_edit', {'id': bon.id}) }}">edit</a>

    {{ include('bon/_delete_form.html.twig') }}
{% endblock %}
